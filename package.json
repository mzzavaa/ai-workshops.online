{
  "name": "ai-workshops-online",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":   "hugo server --disableFastRender --bind 0.0.0.0",
    "build": "hugo --minify",
    "clean": "rm -rf public"
  }
}
